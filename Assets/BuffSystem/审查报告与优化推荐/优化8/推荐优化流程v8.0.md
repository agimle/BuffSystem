# BuffSystem 推荐优化流程 v8.0

**文档版本：** v8.0  
**更新日期：** 2026-02-13  
**适用范围：** BuffSystem v8.x 版本优化  
**优先级策略：** 不依赖/少依赖功能 > 功能升级 > 性能 > 编辑工具 > 测试 > 文档  
**前置条件：** 已完成v1.0-v7.x所有优化项  
**目标：** 打造行业标杆级Buff系统，达到100%生产就绪度

---

## 目录

1. [优化总览](#优化总览)
2. [Phase 1: 独立功能补充（不依赖/少依赖）](#phase-1-独立功能补充不依赖少依赖)
3. [Phase 2: 功能升级与扩展](#phase-2-功能升级与扩展)
4. [Phase 3: 性能优化与验证](#phase-3-性能优化与验证)
5. [Phase 4: 编辑器工具](#phase-4-编辑器工具)
6. [Phase 5: 测试体系](#phase-5-测试体系)
7. [Phase 6: 文档与示例](#phase-6-文档与示例)
8. [优化实施检查清单](#优化实施检查清单)
9. [版本发布计划](#版本发布计划)
10. [附录：v8.0+ 未来扩展规划](#附录v80-未来扩展规划)

---

## 优化总览

### v8.0优化目标

```
┌─────────────────────────────────────────────────────────────┐
│                    BuffSystem v8.0 优化目标                  │
├─────────────────────────────────────────────────────────────┤
│  目标：按新优先级策略完成系统完善，达到100%生产就绪度       │
├─────────────────────────────────────────────────────────────┤
│  Phase 1: 独立功能补充 - 不依赖其他模块，可并行开发         │
│  Phase 2: 功能升级 - 在现有功能基础上增强                   │
│  Phase 3: 性能优化 - 基于基准测试进行针对性优化             │
│  Phase 4: 编辑器工具 - 提升开发效率的可视化工具             │
│  Phase 5: 测试体系 - 单元测试、集成测试覆盖                 │
│  Phase 6: 文档示例 - 完善文档和示例项目                     │
└─────────────────────────────────────────────────────────────┘
```

### 新优先级策略说明

**为什么要调整优先级？**

传统依赖层级（基础设施→文档→功能→工具）会导致前期无法看到实际效果，后期才发现问题。

**新策略优势：**
1. **快速见效** - 先做独立功能，立即看到成果
2. **降低风险** - 功能升级在稳定基础上进行
3. **数据驱动** - 性能优化基于实际测试数据
4. **工具后置** - 编辑器工具在功能稳定后开发，避免反复修改

```
新优先级流程:
独立功能(并行) → 功能升级 → 性能优化 → 编辑器工具 → 测试 → 文档
     │              │          │           │         │      │
     ▼              ▼          ▼           ▼         ▼      ▼
  快速见效      增强能力    数据驱动     效率提升   质量保证  完善交付
```

### v7.x → v8.0演进

| 优化项 | v7.x状态 | v8.0目标 | 优先级 | 所属Phase |
|--------|---------|---------|--------|----------|
| **Phase 1: 独立功能补充** | | | | |
| 条件组合系统 | ⬜ 待完成 | ✅ 完成 | 🔴 极高 | Phase 1 |
| Buff标签哈希优化 | ⬜ 待完成 | ✅ 完成 | 🔴 极高 | Phase 1 |
| 字符串ID支持 | ⬜ 待完成 | ✅ 完成 | 🟡 中 | Phase 1 |
| **Phase 2: 功能升级** | | | | |
| BuffDataSO组配置 | ⬜ 待完成 | ✅ 完成 | 🔴 高 | Phase 2 |
| 条件系统增强 | ⬜ 待完成 | ✅ 完成 | 🔴 高 | Phase 2 |
| Buff继承/模板 | ⬜ 待完成 | ✅ 完成 | 🟡 中 | Phase 2 |
| **Phase 3: 性能优化** | | | | |
| 性能基准测试框架 | ⬜ 待完成 | ✅ 完成 | 🔴 高 | Phase 3 |
| 内存优化 | ⬜ 待完成 | ✅ 完成 | 🟡 中 | Phase 3 |
| Job System支持 | ⬜ 待完成 | 🟡 可选 | 🟢 低 | Phase 3 |
| **Phase 4: 编辑器工具** | | | | |
| 可视化调试窗口 | ⬜ 待完成 | ✅ 完成 | 🔴 高 | Phase 4 |
| 配置验证工具 | ⬜ 待完成 | ✅ 完成 | 🟡 中 | Phase 4 |
| 性能监控器 | ⬜ 待完成 | ✅ 完成 | 🟡 中 | Phase 4 |
| **Phase 5: 测试体系** | | | | |
| 单元测试 | ⬜ 待完成 | ✅ 完成 | 🟡 中 | Phase 5 |
| 集成测试 | ⬜ 待完成 | 🟡 可选 | 🟢 低 | Phase 5 |
| **Phase 6: 文档示例** | | | | |
| API稳定性文档 | ⬜ 待完成 | ✅ 完成 | 🔴 高 | Phase 6 |
| BasicUsage示例 | ⬜ 待完成 | ✅ 完成 | 🔴 高 | Phase 6 |
| RPGSystem示例 | ⬜ 待完成 | ✅ 完成 | 🟡 中 | Phase 6 |
| MOBASystem示例 | ⬜ 待完成 | ✅ 完成 | 🟡 中 | Phase 6 |

---

## Phase 1: 独立功能补充（不依赖/少依赖）

**优先级：** 🔴 极高（可并行开发，快速见效）  
**预估工时：** 5-7天  
**核心目标：** 完成不依赖其他模块的独立功能  
**特点：** 这些功能之间没有依赖关系，可以并行开发！

---

### 🔴 1.1 条件组合系统增强

**优先级：** 🔴 极高（Phase 1第一项）  
**预估工时：** 2天  
**状态：** ⬜ 待完成  
**依赖：** 无（独立功能）  
**为什么先做：** 不依赖其他模块，可立即提升系统灵活性

#### 问题分析

当前条件系统只支持简单条件列表（AND逻辑），不支持复杂组合：

```csharp
// 当前：只能简单AND组合
[SerializeReference]
private List<IBuffCondition> conditions = new();
// 所有条件都必须满足（AND逻辑）
```

#### 解决方案

实现条件组合系统，支持AND/OR/NOT逻辑：

```csharp
namespace BuffSystem.Core
{
    /// <summary>
    /// 条件组合类型
    /// </summary>
    public enum ConditionCombineType
    {
        And,    // 所有条件都必须满足
        Or,     // 任一条件满足即可
        Not     // 条件不满足
    }
    
    /// <summary>
    /// 条件组合 - 支持复杂逻辑
    /// </summary>
    [Serializable]
    public class ConditionGroup : IBuffCondition
    {
        [SerializeField] private ConditionCombineType combineType = ConditionCombineType.And;
        [SerializeReference, SubclassSelector]
        private List<IBuffCondition> conditions = new();
        
        public bool CheckCondition(IBuffOwner owner, IBuffData data)
        {
            switch (combineType)
            {
                case ConditionCombineType.And:
                    return conditions.All(c => c.CheckCondition(owner, data));
                case ConditionCombineType.Or:
                    return conditions.Any(c => c.CheckCondition(owner, data));
                case ConditionCombineType.Not:
                    return !conditions.Any(c => c.CheckCondition(owner, data));
                default:
                    return true;
            }
        }
    }
    
    /// <summary>
    /// 条件组合扩展方法
    /// </summary>
    public static class ConditionExtensions
    {
        public static IBuffCondition And(params IBuffCondition[] conditions)
            => new ConditionGroup { CombineType = ConditionCombineType.And, Conditions = conditions.ToList() };
            
        public static IBuffCondition Or(params IBuffCondition[] conditions)
            => new ConditionGroup { CombineType = ConditionCombineType.Or, Conditions = conditions.ToList() };
            
        public static IBuffCondition Not(this IBuffCondition condition)
            => new ConditionGroup { CombineType = ConditionCombineType.Not, Conditions = new List<IBuffCondition> { condition } };
    }
}
```

**使用示例：**

```csharp
// 复杂条件：需要满足条件A AND (条件B OR 条件C) AND NOT 条件D
var complexCondition = ConditionA.And(
    ConditionB.Or(ConditionC),
    ConditionD.Not()
);

// 在BuffDataSO中使用
[SerializeReference, SubclassSelector]
private ConditionGroup addConditions;
```

**验收标准：**
- [ ] 实现ConditionGroup类
- [ ] 支持And/Or/Not三种组合类型
- [ ] 支持嵌套组合（组合中包含组合）
- [ ] 提供Fluent API扩展方法
- [ ] 在BuffDataSO编辑器中正确显示
- [ ] 编写使用示例和文档

---

### 🔴 1.2 Buff标签哈希优化

**优先级：** 🔴 极高（Phase 1第二项）  
**预估工时：** 1天  
**状态：** ⬜ 待完成  
**依赖：** 无（独立功能）  
**为什么先做：** 性能优化，零依赖，立即见效

#### 问题分析

当前标签系统使用字符串比较，存在性能问题：

```csharp
// 当前：字符串比较，GC和性能开销
public bool HasTag(string tag) => tags.Contains(tag); // O(n)字符串比较
```

#### 解决方案

使用int哈希替代字符串，保持API兼容：

```csharp
namespace BuffSystem.Data
{
    /// <summary>
    /// Buff标签 - 使用哈希优化性能
    /// </summary>
    [Serializable]
    public struct BuffTag : IEquatable<BuffTag>
    {
        [SerializeField] private string tagName;
        private int tagHash;
        
        public string TagName => tagName;
        public int TagHash => tagHash;
        
        public BuffTag(string name)
        {
            tagName = name;
            tagHash = name.GetHashCode();
        }
        
        public bool Equals(BuffTag other) => tagHash == other.tagHash;
        public override bool Equals(object obj) => obj is BuffTag other && Equals(other);
        public override int GetHashCode() => tagHash;
        
        public static implicit operator BuffTag(string name) => new BuffTag(name);
        public static implicit operator string(BuffTag tag) => tag.tagName;
    }
    
    /// <summary>
    /// 标签管理器 - 全局标签哈希缓存
    /// </summary>
    public static class BuffTagManager
    {
        private static readonly Dictionary<string, int> tagHashes = new();
        
        public static int GetTagHash(string tagName)
        {
            if (!tagHashes.TryGetValue(tagName, out var hash))
            {
                hash = tagName.GetHashCode();
                tagHashes[tagName] = hash;
            }
            return hash;
        }
    }
}
```

**BuffDataSO修改：**

```csharp
[Header("标签")]
[SerializeField] private List<BuffTag> tags = new();

// 优化后的HasTag方法
public bool HasTag(string tag)
{
    int hash = BuffTagManager.GetTagHash(tag);
    return tags.Any(t => t.TagHash == hash); // O(1)哈希比较
}

public bool HasTag(BuffTag tag)
{
    return tags.Any(t => t.TagHash == tag.TagHash);
}
```

**验收标准：**
- [ ] 创建BuffTag结构体
- [ ] 创建BuffTagManager全局缓存
- [ ] 修改BuffDataSO使用BuffTag
- [ ] 保持向后兼容（支持string隐式转换）
- [ ] 性能测试：对比字符串vs哈希查询性能
- [ ] 更新相关工具类（BuffQueryHelper等）

---

### 🟡 1.3 字符串ID支持

**优先级：** 🟡 中（Phase 1第三项）  
**预估工时：** 1天  
**状态：** ⬜ 待完成  
**依赖：** 无（独立功能）  
**为什么后做：** 增强功能，非核心需求

#### 问题分析

当前只支持int类型的BuffID，配置时不够直观：

```csharp
// 当前：必须使用数字ID
BuffApi.AddBuff(1001, player); // 1001是什么Buff？
```

#### 解决方案

支持字符串ID，自动映射到int：

```csharp
namespace BuffSystem.Data
{
    /// <summary>
    /// BuffID - 支持int和string
    /// </summary>
    [Serializable]
    public struct BuffId : IEquatable<BuffId>
    {
        [SerializeField] private int id;
        [SerializeField] private string stringId;
        
        public int Id => id;
        public string StringId => stringId;
        
        public BuffId(int id)
        {
            this.id = id;
            this.stringId = null;
        }
        
        public BuffId(string stringId)
        {
            this.stringId = stringId;
            this.id = stringId.GetHashCode(); // 字符串哈希作为ID
        }
        
        public bool Equals(BuffId other) => id == other.id;
        public override bool Equals(object obj) => obj is BuffId other && Equals(other);
        public override int GetHashCode() => id;
        
        public static implicit operator BuffId(int id) => new BuffId(id);
        public static implicit operator BuffId(string id) => new BuffId(id);
        public static implicit operator int(BuffId id) => id.id;
    }
}
```

**BuffApi扩展：**

```csharp
public static class BuffApi
{
    // 支持字符串ID
    public static IBuff AddBuff(string buffName, IBuffOwner target, object source = null)
    {
        var data = BuffDatabase.Instance.GetBuffData(buffName);
        // ...
    }
    
    // 支持BuffId结构体
    public static IBuff AddBuff(BuffId buffId, IBuffOwner target, object source = null)
    {
        // ...
    }
}
```

**使用示例：**

```csharp
// 字符串ID（推荐）
BuffApi.AddBuff("FireDot", player);
BuffApi.AddBuff("IceShield", player);

// 混合使用
BuffApi.AddBuff(1001, player);      // int
BuffApi.AddBuff("FireDot", player); // string
```

**验收标准：**
- [ ] 创建BuffId结构体
- [ ] 修改BuffDataSO支持字符串ID
- [ ] 修改BuffDatabase支持字符串查询
- [ ] 扩展BuffApi支持字符串ID
- [ ] 保持向后兼容
- [ ] 提供ID冲突检测工具

---

### 🟡 1.4 其他独立功能（可选）

| 功能 | 说明 | 工时 | 优先级 |
|------|------|------|--------|
| 常用条件预设 | HealthAbove, ManaAbove, HasBuff等预设条件 | 1天 | 🟡 中 |
| Buff触发器系统 | 基于事件的触发器（血量低于30%触发） | 2天 | 🟡 中 |
| 时间条件扩展 | 游戏时间、现实时间条件 | 1天 | 🟢 低 |

---

## Phase 2: 功能升级与扩展

**优先级：** 🔴 高（基于现有功能增强）  
**预估工时：** 7-10天  
**核心目标：** 在现有稳定功能基础上进行增强  
**依赖：** Phase 1完成（使用新的条件系统、标签系统等）

---

### 🔴 2.1 BuffDataSO支持组配置

**优先级：** 🔴 高（Phase 2第一项）  
**预估工时：** 2天  
**状态：** ⬜ 待完成  
**依赖：** Phase 1.2 标签哈希优化  
**为什么先做：** 完成v7.0未完成项，提升配置能力

#### 问题分析

Buff组系统已实现，但BuffDataSO还不支持在配置中指定所属组：

```csharp
// 当前：Buff组需要在代码中动态添加
// 期望：在SO配置中直接指定组
```

#### 解决方案

在BuffDataSO中添加组配置：

```csharp
namespace BuffSystem.Data
{
    [CreateAssetMenu(fileName = "NewBuffData", menuName = "BuffSystem/Buff Data", order = 1)]
    public class BuffDataSO : ScriptableObject, IBuffData
    {
        // ... 现有字段 ...
        
        [Header("组配置")]
        [Tooltip("所属Buff组ID列表")]
        [SerializeField] private List<string> groupIds = new();
        
        [Tooltip("组内最大层数（0表示使用全局配置）")]
        [SerializeField] private int groupMaxStack;
        
        [Tooltip("组策略")]
        [SerializeField] private GroupStrategyType groupStrategy = GroupStrategyType.AddToGroup;
        
        // 组配置属性
        public IReadOnlyList<string> GroupIds => groupIds;
        public int GroupMaxStack => groupMaxStack;
        public GroupStrategyType GroupStrategy => groupStrategy;
    }
    
    /// <summary>
    /// 组策略类型
    /// </summary>
    public enum GroupStrategyType
    {
        AddToGroup,      // 添加到组
        ReplaceOldest,   // 替换组内最老的Buff
        ReplaceWeakest,  // 替换组内最弱的Buff
        BlockIfFull      // 组满时阻止添加
    }
}
```

**BuffContainer修改：**

```csharp
public class BuffContainer : IBuffContainer
{
    public IBuff AddBuff(IBuffData data, object source, IEnumerable<IBuffModifier> modifiers)
    {
        // ... 现有代码 ...
        
        // 处理组配置
        if (data is BuffDataSO buffDataSO && buffDataSO.GroupIds.Count > 0)
        {
            foreach (var groupId in buffDataSO.GroupIds)
            {
                if (!HandleGroupStrategy(groupId, buffDataSO, out var blocked))
                {
                    if (blocked) return null;
                }
            }
        }
        
        // ... 继续添加Buff ...
    }
    
    private bool HandleGroupStrategy(string groupId, BuffDataSO data, out bool blocked)
    {
        blocked = false;
        var group = GetOrCreateGroup(groupId);
        
        switch (data.GroupStrategy)
        {
            case GroupStrategyType.BlockIfFull:
                if (group.Count >= (data.GroupMaxStack > 0 ? data.GroupMaxStack : group.MaxSize))
                {
                    blocked = true;
                    return false;
                }
                break;
                
            case GroupStrategyType.ReplaceOldest:
                if (group.IsFull)
                {
                    var oldest = group.GetOldestBuff();
                    RemoveBuff(oldest);
                }
                break;
                
            // ... 其他策略 ...
        }
        
        return true;
    }
}
```

**验收标准：**
- [ ] BuffDataSO添加组配置字段
- [ ] BuffContainer支持组策略处理
- [ ] 实现所有组策略类型
- [ ] 在编辑器中可视化组配置
- [ ] 编写使用示例

---

### 🔴 2.2 条件系统增强

**优先级：** 🔴 高（Phase 2第二项）  
**预估工时：** 2天  
**状态：** ⬜ 待完成  
**依赖：** Phase 1.1 条件组合系统  
**为什么后做：** 基于新的条件组合系统进行增强

#### 新增条件类型

```csharp
namespace BuffSystem.Core.Conditions
{
    /// <summary>
    /// 健康值条件
    /// </summary>
    [Serializable]
    public class HealthCondition : IBuffCondition
    {
        public enum CompareType { GreaterThan, LessThan, Equal, PercentageGreater, PercentageLess }
        
        [SerializeField] private CompareType compareType = CompareType.GreaterThan;
        [SerializeField] private float value = 0.5f;
        
        public bool CheckCondition(IBuffOwner owner, IBuffData data)
        {
            if (!(owner is IHealthOwner healthOwner)) return false;
            
            return compareType switch
            {
                CompareType.GreaterThan => healthOwner.CurrentHealth > value,
                CompareType.LessThan => healthOwner.CurrentHealth < value,
                CompareType.PercentageGreater => healthOwner.HealthPercentage > value,
                CompareType.PercentageLess => healthOwner.HealthPercentage < value,
                _ => false
            };
        }
    }
    
    /// <summary>
    /// 拥有Buff条件
    /// </summary>
    [Serializable]
    public class HasBuffCondition : IBuffCondition
    {
        [SerializeField] private int buffId;
        [SerializeField] private int minStack = 1;
        [SerializeField] private bool checkSelf = true; // true=检查自己, false=检查目标
        
        public bool CheckCondition(IBuffOwner owner, IBuffData data)
        {
            var target = checkSelf ? owner : data.Target; // 需要扩展IBuffData支持Target
            return target?.BuffContainer?.GetBuff(buffId)?.CurrentStack >= minStack;
        }
    }
    
    /// <summary>
    /// 概率条件
    /// </summary>
    [Serializable]
    public class ChanceCondition : IBuffCondition
    {
        [SerializeField] [Range(0f, 1f)] private float chance = 0.5f;
        [SerializeField] private bool useLuckModifier = true;
        
        public bool CheckCondition(IBuffOwner owner, IBuffData data)
        {
            float finalChance = chance;
            if (useLuckModifier && owner is ILuckOwner luckOwner)
            {
                finalChance *= luckOwner.LuckModifier;
            }
            return UnityEngine.Random.value < finalChance;
        }
    }
    
    /// <summary>
    /// 时间条件
    /// </summary>
    [Serializable]
    public class TimeCondition : IBuffCondition
    {
        public enum TimeType { DayTime, NightTime, SpecificHour }
        
        [SerializeField] private TimeType timeType;
        [SerializeField] private int startHour;
        [SerializeField] private int endHour;
        
        public bool CheckCondition(IBuffOwner owner, IBuffData data)
        {
            var currentHour = DateTime.Now.Hour;
            
            return timeType switch
            {
                TimeType.DayTime => currentHour >= 6 && currentHour < 18,
                TimeType.NightTime => currentHour < 6 || currentHour >= 18,
                TimeType.SpecificHour => currentHour >= startHour && currentHour < endHour,
                _ => false
            };
        }
    }
}
```

**验收标准：**
- [ ] 实现HealthCondition
- [ ] 实现HasBuffCondition
- [ ] 实现ChanceCondition
- [ ] 实现TimeCondition
- [ ] 所有条件支持在编辑器中配置
- [ ] 提供条件模板预设

---

### 🟡 2.3 Buff继承/模板系统

**优先级：** 🟡 中（Phase 2第三项）  
**预估工时：** 3天  
**状态：** ⬜ 待完成  
**依赖：** 无  
**为什么后做：** 高级功能，提升配置便利性

#### 设计方案

```csharp
namespace BuffSystem.Data
{
    /// <summary>
    /// Buff模板 - 可继承的基础配置
    /// </summary>
    [CreateAssetMenu(fileName = "NewBuffTemplate", menuName = "BuffSystem/Buff Template", order = 0)]
    public class BuffTemplate : ScriptableObject
    {
        [Header("基础模板")]
        [SerializeField] private BuffEffectType effectType = BuffEffectType.Neutral;
        [SerializeField] private BuffStackMode stackMode = BuffStackMode.Stackable;
        [SerializeField] private int maxStack = 1;
        [SerializeField] private float duration = 5f;
        [SerializeField] private bool canRefresh = true;
        [SerializeField] private BuffRemoveMode removeMode = BuffRemoveMode.Remove;
        [SerializeField] private List<string> defaultTags = new();
        [SerializeReference] private BuffLogicBase defaultLogic;
        
        // 应用模板到BuffData
        public void ApplyTemplate(BuffDataSO target)
        {
            // 只应用未设置的字段
            if (target.EffectType == default) target.SetEffectType(effectType);
            if (target.StackMode == default) target.SetStackMode(stackMode);
            // ...
        }
    }
    
    /// <summary>
    /// BuffDataSO扩展 - 支持模板继承
    /// </summary>
    public partial class BuffDataSO
    {
        [Header("模板继承")]
        [SerializeField] private BuffTemplate baseTemplate;
        [SerializeField] private bool overrideTemplateValues = false;
        
        [Header("属性覆盖")]
        [SerializeField] private List<PropertyOverride> propertyOverrides = new();
        
        private void OnEnable()
        {
            if (baseTemplate != null && !overrideTemplateValues)
            {
                baseTemplate.ApplyTemplate(this);
            }
        }
    }
    
    /// <summary>
    /// 属性覆盖
    /// </summary>
    [Serializable]
    public class PropertyOverride
    {
        public string propertyName;
        public float floatValue;
        public int intValue;
        public bool boolValue;
    }
}
```

**验收标准：**
- [ ] 创建BuffTemplate类
- [ ] BuffDataSO支持模板继承
- [ ] 支持属性覆盖
- [ ] 编辑器中可视化继承关系
- [ ] 模板变更自动更新子Buff

---

## Phase 3: 性能优化与验证

**优先级：** 🔴 高（基于测试数据优化）  
**预估工时：** 5-7天  
**核心目标：** 建立性能基准，针对性优化  
**依赖：** Phase 1-2完成（功能稳定后才能准确测试）

---

### 🔴 3.1 性能基准测试框架

**优先级：** 🔴 极高（Phase 3第一项）  
**预估工时：** 3天  
**状态：** ⬜ 待完成  
**依赖：** Phase 1-2完成  
**为什么先做：** 必须先有测量工具，才能知道优化效果

#### 测试框架设计

```csharp
namespace BuffSystem.Tests
{
    /// <summary>
    /// 性能测试基类
    /// </summary>
    public abstract class BuffPerformanceTestBase
    {
        protected Stopwatch stopwatch = new();
        protected long gcBefore;
        protected long gcAfter;
        
        protected void BeginSample()
        {
            GC.Collect();
            GC.WaitForPendingFinalizers();
            gcBefore = GC.GetTotalMemory(false);
            stopwatch.Restart();
        }
        
        protected PerformanceResult EndSample()
        {
            stopwatch.Stop();
            gcAfter = GC.GetTotalMemory(false);
            
            return new PerformanceResult
            {
                ElapsedMilliseconds = stopwatch.ElapsedMilliseconds,
                AllocatedBytes = gcAfter - gcBefore,
                SampleCount = 1
            };
        }
    }
    
    /// <summary>
    /// 性能测试结果
    /// </summary>
    public struct PerformanceResult
    {
        public long ElapsedMilliseconds;
        public long AllocatedBytes;
        public int SampleCount;
        
        public double AverageMs => ElapsedMilliseconds / (double)SampleCount;
        public double AverageBytes => AllocatedBytes / (double)SampleCount;
    }
    
    /// <summary>
    /// Buff系统性能测试
    /// </summary>
    public class BuffSystemPerformanceTests : BuffPerformanceTestBase
    {
        #region 基准测试
        
        [PerformanceTest("添加Buff基准", 1000)]
        public PerformanceResult Benchmark_AddBuff()
        {
            var owner = CreateTestOwner();
            var data = CreateTestBuffData();
            
            BeginSample();
            for (int i = 0; i < 1000; i++)
            {
                owner.BuffContainer.AddBuff(data);
            }
            return EndSample();
        }
        
        [PerformanceTest("Update基准", 1000)]
        public PerformanceResult Benchmark_Update()
        {
            var owner = CreateTestOwner();
            var data = CreateTestBuffData();
            
            // 预创建1000个Buff
            for (int i = 0; i < 1000; i++)
            {
                owner.BuffContainer.AddBuff(data);
            }
            
            BeginSample();
            owner.BuffContainer.Update(0.016f); // 模拟一帧
            return EndSample();
        }
        
        [PerformanceTest("查询Buff基准", 10000)]
        public PerformanceResult Benchmark_Query()
        {
            var owner = CreateTestOwner();
            SetupTestBuffs(owner, 100);
            
            BeginSample();
            for (int i = 0; i < 10000; i++)
            {
                owner.BuffContainer.GetBuff(1);
            }
            return EndSample();
        }
        
        #endregion
        
        #region 对比测试
        
        [ComparisonTest("Class vs Struct容器")]
        public void Compare_ClassVsStruct()
        {
            var classResult = TestClassContainer();
            var structResult = TestStructContainer();
            
            LogComparison("Class容器", classResult, "Struct容器", structResult);
        }
        
        [ComparisonTest("频率更新对比")]
        public void Compare_FrequencyUpdate()
        {
            var normalResult = TestNormalUpdate();
            var frequencyResult = TestFrequencyUpdate();
            
            LogComparison("普通更新", normalResult, "分层更新", frequencyResult);
        }
        
        #endregion
        
        #region 压力测试
        
        [StressTest("10000 Buff压力测试")]
        public void StressTest_10000Buffs()
        {
            var owner = CreateTestOwner();
            var data = CreateTestBuffData();
            
            // 创建10000个Buff
            for (int i = 0; i < 10000; i++)
            {
                owner.BuffContainer.AddBuff(data);
            }
            
            var result = MeasureUpdate(owner);
            Assert.Less(result.AverageMs, 5.0, "10000 Buff更新应小于5ms");
        }
        
        [StressTest("100 Owner压力测试")]
        public void StressTest_100Owners()
        {
            var owners = new List<IBuffOwner>();
            for (int i = 0; i < 100; i++)
            {
                owners.Add(CreateTestOwner());
            }
            
            // 每个Owner添加100个Buff
            var data = CreateTestBuffData();
            foreach (var owner in owners)
            {
                for (int j = 0; j < 100; j++)
                {
                    owner.BuffContainer.AddBuff(data);
                }
            }
            
            var result = MeasureUpdateAll(owners);
            Assert.Less(result.AverageMs, 10.0, "100 Owner更新应小于10ms");
        }
        
        #endregion
        
        #region 内存测试
        
        [MemoryTest("Buff内存占用")]
        public void Memory_BuffSize()
        {
            var buff = CreateTestBuff();
            var size = GetObjectSize(buff);
            
            Log($"单个Buff内存占用: {size} bytes");
            Assert.Less(size, 64, "Buff内存占用应小于64 bytes");
        }
        
        [MemoryTest("GC Alloc测试")]
        public void Memory_GCAlloc()
        {
            var owner = CreateTestOwner();
            var data = CreateTestBuffData();
            
            GC.Collect();
            var before = GC.CollectionCount(0);
            
            // 执行1000次操作
            for (int i = 0; i < 1000; i++)
            {
                var buff = owner.BuffContainer.AddBuff(data);
                owner.BuffContainer.RemoveBuff(buff);
            }
            
            var after = GC.CollectionCount(0);
            Assert.AreEqual(before, after, "不应触发GC");
        }
        
        #endregion
    }
}
```

**性能报告生成器：**

```csharp
#if UNITY_EDITOR
namespace BuffSystem.Editor
{
    /// <summary>
    /// 性能报告生成器
    /// </summary>
    public class PerformanceReportGenerator
    {
        [MenuItem("BuffSystem/Generate Performance Report")]
        public static void GenerateReport()
        {
            var tests = new BuffSystemPerformanceTests();
            var results = RunAllTests(tests);
            var report = FormatReport(results);
            
            File.WriteAllText("Assets/BuffSystem/PerformanceReport.md", report);
            Debug.Log("性能报告已生成: Assets/BuffSystem/PerformanceReport.md");
        }
        
        private static string FormatReport(List<TestResult> results)
        {
            var sb = new StringBuilder();
            sb.AppendLine("# BuffSystem 性能测试报告");
            sb.AppendLine($"生成时间: {DateTime.Now}");
            sb.AppendLine($"Unity版本: {Application.unityVersion}");
            sb.AppendLine();
            
            sb.AppendLine("## 测试结果汇总");
            sb.AppendLine();
            sb.AppendLine("| 测试项 | 平均耗时 | 内存分配 | 状态 |");
            sb.AppendLine("|--------|----------|----------|------|");
            
            foreach (var result in results)
            {
                sb.AppendLine($"| {result.Name} | {result.AverageMs:F2}ms | {result.AllocatedBytes}B | {(result.Passed ? "✅通过" : "❌失败")} |");
            }
            
            return sb.ToString();
        }
    }
}
#endif
```

**验收标准：**
- [ ] 创建性能测试基类
- [ ] 实现5项基准测试
- [ ] 实现对比测试框架
- [ ] 实现压力测试
- [ ] 实现内存测试
- [ ] 生成性能报告
- [ ] 所有测试可自动化运行

---

### 🟡 3.2 内存优化

**优先级：** 🟡 中（Phase 3第二项）  
**预估工时：** 2天  
**状态：** ⬜ 待完成  
**依赖：** Phase 3.1 性能基准测试  
**为什么后做：** 基于测试数据针对性优化

#### 优化项

| 优化点 | 当前状态 | 目标 | 方案 |
|--------|---------|------|------|
| List预分配 | 动态扩容 | 预分配容量 | 使用EnsureCapacity |
| 字典遍历 | Values集合 | 直接遍历 | 优化遍历方式 |
| 字符串缓存 | 重复创建 | 全局缓存 | BuffTagManager |
| 事件参数 | 类 | 结构体 | 使用struct |

---

### 🟢 3.3 Job System支持（可选）

**优先级：** 🟢 低（Phase 3第三项）  
**预估工时：** 3天  
**状态：** ⬜ 待完成  
**依赖：** Phase 3.1 性能基准测试  
**为什么后做：** 高级优化，需要Burst编译器

---

## Phase 4: 编辑器工具

**优先级：** 🔴 高（提升开发效率）  
**预估工时：** 7-10天  
**核心目标：** 提供可视化调试和配置工具  
**依赖：** Phase 1-3完成（功能稳定后开发工具）

---

### 🔴 4.1 可视化调试窗口

**优先级：** 🔴 极高（Phase 4第一项）  
**预估工时：** 3天  
**状态：** ⬜ 待完成  
**依赖：** Phase 1-3完成  
**为什么先做：** 最重要的开发工具

#### 功能设计

```csharp
#if UNITY_EDITOR
namespace BuffSystem.Editor
{
    /// <summary>
    /// Buff系统调试窗口
    /// </summary>
    public class BuffSystemDebugger : EditorWindow
    {
        private Vector2 ownerListScroll;
        private Vector2 buffListScroll;
        private Vector2 eventLogScroll;
        
        private IBuffOwner selectedOwner;
        private IBuff selectedBuff;
        
        private bool autoRefresh = true;
        private float refreshInterval = 0.5f;
        private float lastRefreshTime;
        
        private List<string> eventLogs = new();
        private bool showEventLog = true;
        
        [MenuItem("Window/BuffSystem/Buff Debugger")]
        public static void ShowWindow()
        {
            GetWindow<BuffSystemDebugger>("Buff Debugger");
        }
        
        private void OnGUI()
        {
            DrawToolbar();
            EditorGUILayout.Space();
            
            EditorGUILayout.BeginHorizontal();
            {
                // 左侧面板：Owner列表
                EditorGUILayout.BeginVertical(GUILayout.Width(250));
                DrawOwnerList();
                EditorGUILayout.EndVertical();
                
                EditorGUILayout.BeginVertical();
                {
                    // 中间面板：Buff详情
                    DrawBuffDetails();
                    
                    EditorGUILayout.Space();
                    
                    // 底部面板：事件日志
                    if (showEventLog)
                    {
                        DrawEventLog();
                    }
                }
                EditorGUILayout.EndVertical();
            }
            EditorGUILayout.EndHorizontal();
            
            // 自动刷新
            if (autoRefresh && Time.realtimeSinceStartup - lastRefreshTime > refreshInterval)
            {
                Repaint();
                lastRefreshTime = Time.realtimeSinceStartup;
            }
        }
        
        private void DrawToolbar()
        {
            EditorGUILayout.BeginHorizontal(EditorStyles.toolbar);
            {
                autoRefresh = GUILayout.Toggle(autoRefresh, "自动刷新", EditorStyles.toolbarButton);
                
                GUILayout.FlexibleSpace();
                
                if (GUILayout.Button("刷新", EditorStyles.toolbarButton))
                {
                    Repaint();
                }
                
                showEventLog = GUILayout.Toggle(showEventLog, "显示日志", EditorStyles.toolbarButton);
            }
            EditorGUILayout.EndHorizontal();
        }
        
        private void DrawOwnerList()
        {
            EditorGUILayout.LabelField("Buff Owners", EditorStyles.boldLabel);
            
            ownerListScroll = EditorGUILayout.BeginScrollView(ownerListScroll, GUI.skin.box);
            {
                foreach (var owner in BuffOwner.AllOwners)
                {
                    var style = owner == selectedOwner ? EditorStyles.selectionRect : GUIStyle.none;
                    
                    EditorGUILayout.BeginHorizontal(style);
                    {
                        if (GUILayout.Button($"{owner.OwnerName} ({owner.BuffCount})", EditorStyles.label))
                        {
                            selectedOwner = owner;
                        }
                    }
                    EditorGUILayout.EndHorizontal();
                }
            }
            EditorGUILayout.EndScrollView();
        }
        
        private void DrawBuffDetails()
        {
            if (selectedOwner == null)
            {
                EditorGUILayout.HelpBox("选择一个BuffOwner查看详情", MessageType.Info);
                return;
            }
            
            EditorGUILayout.LabelField($"Owner: {selectedOwner.OwnerName}", EditorStyles.boldLabel);
            
            buffListScroll = EditorGUILayout.BeginScrollView(buffListScroll, GUI.skin.box, GUILayout.Height(200));
            {
                foreach (var buff in selectedOwner.BuffContainer.AllBuffs)
                {
                    DrawBuffItem(buff);
                }
            }
            EditorGUILayout.EndScrollView();
            
            // 手动操作按钮
            EditorGUILayout.BeginHorizontal();
            {
                if (GUILayout.Button("添加Buff"))
                {
                    ShowAddBuffDialog();
                }
                
                if (GUILayout.Button("清空Buff") && selectedBuff != null)
                {
                    selectedOwner.BuffContainer.Clear();
                }
            }
            EditorGUILayout.EndHorizontal();
        }
        
        private void DrawBuffItem(IBuff buff)
        {
            var style = buff == selectedBuff ? EditorStyles.selectionRect : GUIStyle.none;
            
            EditorGUILayout.BeginHorizontal(style);
            {
                EditorGUILayout.LabelField(buff.Name, GUILayout.Width(150));
                EditorGUILayout.LabelField($"Stack: {buff.CurrentStack}/{buff.MaxStack}", GUILayout.Width(80));
                EditorGUILayout.LabelField($"Time: {buff.RemainingTime:F1}s", GUILayout.Width(80));
                
                if (GUILayout.Button("+", GUILayout.Width(25)))
                {
                    buff.AddStack(1);
                }
                
                if (GUILayout.Button("-", GUILayout.Width(25)))
                {
                    buff.RemoveStack(1);
                }
                
                if (GUILayout.Button("X", GUILayout.Width(25)))
                {
                    buff.MarkForRemoval();
                }
                
                if (GUILayout.Button("选择", GUILayout.Width(50)))
                {
                    selectedBuff = buff;
                }
            }
            EditorGUILayout.EndHorizontal();
        }
        
        private void DrawEventLog()
        {
            EditorGUILayout.LabelField("事件日志", EditorStyles.boldLabel);
            
            eventLogScroll = EditorGUILayout.BeginScrollView(eventLogScroll, GUI.skin.box, GUILayout.Height(150));
            {
                foreach (var log in eventLogs.TakeLast(50))
                {
                    EditorGUILayout.LabelField(log, EditorStyles.miniLabel);
                }
            }
            EditorGUILayout.EndScrollView();
        }
        
        private void ShowAddBuffDialog()
        {
            // 显示添加Buff对话框
        }
    }
}
#endif
```

**验收标准：**
- [ ] 显示所有BuffOwner列表
- [ ] 显示选中Owner的所有Buff
- [ ] 显示Buff详细信息（层数、持续时间等）
- [ ] 支持手动添加Buff
- [ ] 支持手动移除Buff
- [ ] 支持修改层数
- [ ] 显示事件日志
- [ ] 支持自动刷新

---

### 🟡 4.2 配置验证工具

**优先级：** 🟡 中（Phase 4第二项）  
**预估工时：** 2天  
**状态：** ⬜ 待完成  
**依赖：** Phase 4.1 可视化调试窗口  

#### 功能设计

```csharp
#if UNITY_EDITOR
namespace BuffSystem.Editor
{
    /// <summary>
    /// Buff配置验证器
    /// </summary>
    public class BuffConfigValidator : EditorWindow
    {
        [MenuItem("BuffSystem/Validate Configurations")]
        public static void ShowWindow()
        {
            GetWindow<BuffConfigValidator>("Config Validator");
        }
        
        private List<ValidationResult> results = new();
        
        private void OnGUI()
        {
            EditorGUILayout.LabelField("Buff配置验证", EditorStyles.boldLabel);
            
            if (GUILayout.Button("验证所有配置"))
            {
                ValidateAll();
            }
            
            EditorGUILayout.Space();
            
            // 显示验证结果
            foreach (var result in results)
            {
                DrawValidationResult(result);
            }
        }
        
        private void ValidateAll()
        {
            results.Clear();
            
            var allBuffs = LoadAllBuffData();
            
            foreach (var buff in allBuffs)
            {
                // 检查ID冲突
                CheckIdConflict(buff, allBuffs);
                
                // 检查循环依赖
                CheckCircularDependency(buff, new List<int>());
                
                // 检查无效引用
                CheckInvalidReferences(buff);
                
                // 检查性能问题
                CheckPerformanceIssues(buff);
            }
        }
        
        private void CheckIdConflict(BuffDataSO buff, List<BuffDataSO> allBuffs)
        {
            var duplicates = allBuffs.Where(b => b != buff && b.Id == buff.Id);
            if (duplicates.Any())
            {
                results.Add(new ValidationResult
                {
                    Type = ValidationType.Error,
                    Message = $"ID冲突: {buff.Name} 与 {string.Join(", ", duplicates.Select(d => d.Name))} 使用相同ID {buff.Id}",
                    Target = buff
                });
            }
        }
        
        private void CheckCircularDependency(BuffDataSO buff, List<int> visited)
        {
            if (visited.Contains(buff.Id))
            {
                results.Add(new ValidationResult
                {
                    Type = ValidationType.Error,
                    Message = $"循环依赖检测: {buff.Name} 存在循环依赖",
                    Target = buff
                });
                return;
            }
            
            visited.Add(buff.Id);
            
            foreach (var depId in buff.DependBuffIds)
            {
                var dep = BuffDatabase.Instance.GetBuffData(depId);
                if (dep != null)
                {
                    CheckCircularDependency(dep as BuffDataSO, new List<int>(visited));
                }
            }
        }
        
        private void CheckInvalidReferences(BuffDataSO buff)
        {
            foreach (var mutexId in buff.MutexBuffIds)
            {
                if (BuffDatabase.Instance.GetBuffData(mutexId) == null)
                {
                    results.Add(new ValidationResult
                    {
                        Type = ValidationType.Warning,
                        Message = $"无效引用: {buff.Name} 的互斥Buff ID {mutexId} 不存在",
                        Target = buff
                    });
                }
            }
        }
        
        private void CheckPerformanceIssues(BuffDataSO buff)
        {
            // 检查更新频率设置
            if (buff.UpdateFrequency == UpdateFrequency.EveryFrame && buff.Duration > 60f)
            {
                results.Add(new ValidationResult
                {
                    Type = ValidationType.Warning,
                    Message = $"性能建议: {buff.Name} 持续时间较长但使用每帧更新，建议降低更新频率",
                    Target = buff
                });
            }
        }
    }
    
    public enum ValidationType { Error, Warning, Info }
    
    public class ValidationResult
    {
        public ValidationType Type;
        public string Message;
        public UnityEngine.Object Target;
    }
}
#endif
```

**验收标准：**
- [ ] 检测ID冲突
- [ ] 检测循环依赖
- [ ] 检测无效引用
- [ ] 检测性能问题
- [ ] 一键修复功能
- [ ] 生成验证报告

---

### 🟡 4.3 性能监控器

**优先级：** 🟡 中（Phase 4第三项）  
**预估工时：** 1天  
**状态：** ⬜ 待完成  
**依赖：** Phase 3.1 性能基准测试  

#### 设计方案

```csharp
namespace BuffSystem.Runtime
{
    /// <summary>
    /// Buff性能监控器 - 运行时性能显示
    /// </summary>
    [AddComponentMenu("BuffSystem/Performance Monitor")]
    public class BuffPerformanceMonitor : MonoBehaviour
    {
        [Header("显示设置")]
        [SerializeField] private bool showOnScreen = true;
        [SerializeField] private KeyCode toggleKey = KeyCode.F12;
        [SerializeField] private Rect displayRect = new Rect(10, 10, 300, 200);
        
        [Header("监控项")]
        [SerializeField] private bool showBuffCount = true;
        [SerializeField] private bool showOwnerCount = true;
        [SerializeField] private bool showUpdateTime = true;
        [SerializeField] private bool showFPS = true;
        
        private float lastUpdateTime;
        private float currentUpdateTime;
        private float smoothedUpdateTime;
        
        private void Update()
        {
            if (Input.GetKeyDown(toggleKey))
            {
                showOnScreen = !showOnScreen;
            }
        }
        
        private void OnGUI()
        {
            if (!showOnScreen) return;
            
            GUILayout.BeginArea(displayRect, GUI.skin.box);
            {
                GUILayout.Label("=== BuffSystem 性能监控 ===", EditorStyles.boldLabel);
                GUILayout.Space(5);
                
                if (showBuffCount)
                {
                    GUILayout.Label($"Buff总数: {GetTotalBuffCount()}");
                }
                
                if (showOwnerCount)
                {
                    GUILayout.Label($"Owner数量: {BuffOwner.AllOwners.Count}");
                }
                
                if (showUpdateTime)
                {
                    GUILayout.Label($"更新时间: {smoothedUpdateTime:F2}ms");
                }
                
                if (showFPS)
                {
                    GUILayout.Label($"FPS: {1f / Time.unscaledDeltaTime:F0}");
                }
            }
            GUILayout.EndArea();
        }
        
        private int GetTotalBuffCount()
        {
            int count = 0;
            foreach (var owner in BuffOwner.AllOwners)
            {
                count += owner.BuffCount;
            }
            return count;
        }
    }
}
```

**验收标准：**
- [ ] 实时显示Buff数量
- [ ] 实时显示Owner数量
- [ ] 显示更新时间
- [ ] 显示FPS
- [ ] 支持快捷键开关
- [ ] 可配置显示项

---

## Phase 5: 测试体系

**优先级：** 🟡 中（质量保证）  
**预估工时：** 5-7天  
**核心目标：** 建立单元测试和集成测试  
**依赖：** Phase 1-4完成（功能稳定后编写测试）

---

### 🟡 5.1 单元测试

**优先级：** 🟡 中（Phase 5第一项）  
**预估工时：** 4天  
**状态：** ⬜ 待完成  
**依赖：** Phase 1-4完成  

#### 测试覆盖计划

| 模块 | 测试类 | 测试数量 | 优先级 |
|------|--------|---------|--------|
| Core | BuffEntityTests | 15 | 🔴 高 |
| Runtime | BuffContainerTests | 20 | 🔴 高 |
| Data | BuffDataSOTests | 10 | 🟡 中 |
| Events | BuffEventSystemTests | 8 | 🟡 中 |
| Strategy | StrategyTests | 12 | 🟢 低 |

---

### 🟢 5.2 集成测试（可选）

**优先级：** 🟢 低（Phase 5第二项）  
**预估工时：** 2天  
**状态：** ⬜ 待完成  
**依赖：** Phase 5.1 单元测试  

---

## Phase 6: 文档与示例

**优先级：** 🔴 高（完善交付）  
**预估工时：** 7-10天  
**核心目标：** 完善文档体系和示例项目  
**依赖：** Phase 1-5完成（所有功能稳定后编写文档）

---

### 🔴 6.1 API稳定性文档

**优先级：** 🔴 极高（Phase 6第一项）  
**预估工时：** 1天  
**状态：** ⬜ 待完成  
**依赖：** 所有功能完成  

#### 文档结构

```markdown
# BuffSystem API稳定性保证文档

## 稳定API列表 (🔒 Stable)

### Core命名空间
- IBuff (v6.0+)
- IBuffData (v6.0+)
- IBuffLogic (v6.0+)
- BuffApi (v6.0+)

### Runtime命名空间
- BuffOwner (v6.0+)
- BuffContainer (v6.0+)
- BuffEntity (v6.0+)

## 预览API列表 (👁️ Preview)

### Advanced命名空间
- BuffComboManager (v7.0+)
- FusionManager (v7.0+)

## 修改策略

### 稳定API
- 只允许bug修复
- 不允许签名变更
- 不允许行为变更

### 预览API
- 可能有小调整
- 会提前通知
```

---

### 🔴 6.2 BasicUsage示例

**优先级：** 🔴 极高（Phase 6第二项）  
**预估工时：** 2天  
**状态：** ⬜ 待完成  
**依赖：** 所有功能完成  

#### 示例内容

```csharp
namespace BuffSystem.Examples.BasicUsage
{
    /// <summary>
    /// 基础使用示例 - 展示核心API
    /// </summary>
    public class BasicUsageExample : MonoBehaviour
    {
        [Header("配置")]
        [SerializeField] private int healBuffId = 1001;
        [SerializeField] private int damageBuffId = 1002;
        
        private IBuffOwner player;
        
        private void Start()
        {
            // 获取BuffOwner
            player = GetComponent<BuffOwner>();
            
            // 示例1: 添加Buff
            AddBuffExample();
            
            // 示例2: 查询Buff
            QueryBuffExample();
            
            // 示例3: 事件监听
            EventExample();
        }
        
        private void AddBuffExample()
        {
            // 方法1: 通过ID添加
            var buff1 = BuffApi.AddBuff(healBuffId, player);
            
            // 方法2: 通过名称添加
            var buff2 = BuffApi.AddBuff("HealOverTime", player);
            
            // 方法3: 带来源添加
            var buff3 = BuffApi.AddBuff(healBuffId, player, this);
            
            Debug.Log($"添加了 {player.BuffCount} 个Buff");
        }
        
        private void QueryBuffExample()
        {
            // 获取特定Buff
            var healBuff = player.BuffContainer.GetBuff(healBuffId);
            if (healBuff != null)
            {
                Debug.Log($"治疗Buff层数: {healBuff.CurrentStack}");
            }
            
            // 遍历所有Buff
            foreach (var buff in player.BuffContainer.AllBuffs)
            {
                Debug.Log($"Buff: {buff.Name}, 剩余时间: {buff.RemainingTime:F1}s");
            }
        }
        
        private void EventExample()
        {
            // 监听全局事件
            BuffEventSystem.OnBuffAdded += (args) =>
            {
                Debug.Log($"全局事件: {args.Buff.Name} 被添加到 {args.Buff.Owner.OwnerName}");
            };
            
            // 监听本地事件
            player.LocalEvents.OnBuffAdded += (buff) =>
            {
                Debug.Log($"本地事件: {buff.Name} 被添加");
            };
        }
    }
}
```

**验收标准：**
- [ ] 展示所有核心API用法
- [ ] 包含详细注释
- [ ] 可运行场景
- [ ] 配套文档教程

---

### 🟡 6.3 RPGSystem示例

**优先级：** 🟡 中（Phase 6第三项）  
**预估工时：** 3天  
**状态：** ⬜ 待完成  
**依赖：** 6.2 BasicUsage示例  

#### 示例内容

- Hot（持续治疗）效果实现
- DoT（持续伤害）效果实现
- 属性增益效果实现
- 控制效果实现（眩晕、冰冻等）
- Buff UI显示

---

### 🟡 6.4 MOBASystem示例

**优先级：** 🟡 中（Phase 6第四项）  
**预估工时：** 3天  
**状态：** ⬜ 待完成  
**依赖：** 6.2 BasicUsage示例  

#### 示例内容

- 召唤师技能示例
- 装备被动效果示例
- 红蓝Buff示例
- 技能连招示例

---

## 优化实施检查清单

### Phase 1: 独立功能补充（Week 1-2）【可并行开发】

- [ ] **1.1 条件组合系统** (2天)
  - [ ] 1.1.1 实现ConditionGroup类
  - [ ] 1.1.2 支持And/Or/Not三种组合
  - [ ] 1.1.3 支持嵌套组合
  - [ ] 1.1.4 提供Fluent API
  - [ ] 1.1.5 编辑器支持

- [ ] **1.2 Buff标签哈希优化** (1天)
  - [ ] 1.2.1 创建BuffTag结构体
  - [ ] 1.2.2 创建BuffTagManager
  - [ ] 1.2.3 修改BuffDataSO
  - [ ] 1.2.4 性能测试对比

- [ ] **1.3 字符串ID支持** (1天)
  - [ ] 1.3.1 创建BuffId结构体
  - [ ] 1.3.2 修改BuffDatabase
  - [ ] 1.3.3 扩展BuffApi
  - [ ] 1.3.4 ID冲突检测

- [ ] **1.4 其他独立功能** (2天)
  - [ ] 1.4.1 常用条件预设
  - [ ] 1.4.2 Buff触发器系统

### Phase 2: 功能升级（Week 3-4）

- [ ] **2.1 BuffDataSO组配置** (2天)
  - [ ] 2.1.1 添加组配置字段
  - [ ] 2.1.2 实现组策略
  - [ ] 2.1.3 编辑器支持

- [ ] **2.2 条件系统增强** (2天)
  - [ ] 2.2.1 HealthCondition
  - [ ] 2.2.2 HasBuffCondition
  - [ ] 2.2.3 ChanceCondition
  - [ ] 2.2.4 TimeCondition

- [ ] **2.3 Buff继承/模板** (3天)
  - [ ] 2.3.1 BuffTemplate类
  - [ ] 2.3.2 模板继承机制
  - [ ] 2.3.3 属性覆盖

### Phase 3: 性能优化（Week 5）

- [ ] **3.1 性能基准测试框架** (3天)
  - [ ] 3.1.1 测试基类
  - [ ] 3.1.2 5项基准测试
  - [ ] 3.1.3 对比测试
  - [ ] 3.1.4 压力测试
  - [ ] 3.1.5 报告生成

- [ ] **3.2 内存优化** (2天)
  - [ ] 3.2.1 List预分配
  - [ ] 3.2.2 字典遍历优化
  - [ ] 3.2.3 事件参数结构体化

### Phase 4: 编辑器工具（Week 6）

- [ ] **4.1 可视化调试窗口** (3天)
  - [ ] 4.1.1 Owner列表显示
  - [ ] 4.1.2 Buff详情面板
  - [ ] 4.1.3 手动操作功能
  - [ ] 4.1.4 事件日志

- [ ] **4.2 配置验证工具** (2天)
  - [ ] 4.2.1 ID冲突检测
  - [ ] 4.2.2 循环依赖检测
  - [ ] 4.2.3 无效引用检测
  - [ ] 4.2.4 性能问题检测

- [ ] **4.3 性能监控器** (1天)
  - [ ] 4.3.1 运行时数据显示
  - [ ] 4.3.2 快捷键支持

### Phase 5: 测试体系（Week 7）

- [ ] **5.1 单元测试** (4天)
  - [ ] 5.1.1 BuffEntity测试
  - [ ] 5.1.2 BuffContainer测试
  - [ ] 5.1.3 核心功能测试
  - [ ] 5.1.4 覆盖率报告

- [ ] **5.2 集成测试** (2天)
  - [ ] 5.2.1 端到端测试
  - [ ] 5.2.2 场景测试

### Phase 6: 文档示例（Week 8）

- [ ] **6.1 API稳定性文档** (1天)
  - [ ] 6.1.1 稳定API列表
  - [ ] 6.1.2 预览API列表
  - [ ] 6.1.3 修改策略

- [ ] **6.2 BasicUsage示例** (2天)
  - [ ] 6.2.1 示例场景
  - [ ] 6.2.2 示例代码
  - [ ] 6.2.3 详细注释

- [ ] **6.3 RPGSystem示例** (3天)
  - [ ] 6.3.1 Hot/DoT效果
  - [ ] 6.3.2 属性增益
  - [ ] 6.3.3 控制效果
  - [ ] 6.3.4 UI显示

- [ ] **6.4 MOBASystem示例** (3天)
  - [ ] 6.4.1 召唤师技能
  - [ ] 6.4.2 装备被动
  - [ ] 6.4.3 红蓝Buff

---

## 版本发布计划

### v8.0.0 发布内容

```
Phase 1 - 独立功能补充:
├── 条件组合系统（And/Or/Not）
├── Buff标签哈希优化
├── 字符串ID支持
└── 常用条件预设

Phase 2 - 功能升级:
├── BuffDataSO组配置
├── 条件系统增强（Health/HasBuff/Chance/Time）
└── Buff继承/模板系统

Phase 3 - 性能优化:
├── 性能基准测试框架
├── 5项基准测试
├── 对比测试
└── 内存优化

Phase 4 - 编辑器工具:
├── 可视化调试窗口
├── 配置验证工具
└── 性能监控器

Phase 5 - 测试体系:
├── 单元测试框架
├── 核心功能测试覆盖
└── 集成测试

Phase 6 - 文档示例:
├── API稳定性文档
├── BasicUsage示例
├── RPGSystem示例
└── MOBASystem示例
```

### 发布时间表

| Phase | 时间 | 里程碑 | 可交付成果 |
|-------|------|--------|-----------|
| Phase 1 | Week 1-2 | 独立功能完成 | 条件组合、标签优化、字符串ID |
| Phase 2 | Week 3-4 | 功能升级完成 | 组配置、条件增强、模板系统 |
| Phase 3 | Week 5 | 性能优化完成 | 基准测试、内存优化 |
| Phase 4 | Week 6 | 工具链完成 | 调试窗口、验证工具、监控器 |
| Phase 5 | Week 7 | 测试体系完成 | 单元测试、集成测试 |
| Phase 6 | Week 8 | 文档示例完成 | API文档、3个示例项目 |
| 发布 | Week 8末 | v8.0.0发布 | 完整包 |

### 依赖关系图（新优先级）

```
Phase 1 (独立功能) ─────────────────────────────────────────►
    │                    │                    │
    ▼                    ▼                    ▼
条件组合系统          标签哈希优化         字符串ID支持
    │                    │                    │
    └────────────────────┼────────────────────┘
                         ▼
              Phase 2 (功能升级)
                         │
            ┌────────────┼────────────┐
            ▼            ▼            ▼
        组配置      条件增强      模板系统
            │            │            │
            └────────────┼────────────┘
                         ▼
              Phase 3 (性能优化)
                         │
            ┌────────────┼────────────┐
            ▼            ▼            ▼
        基准测试      内存优化      Job支持(可选)
            │            │            │
            └────────────┼────────────┘
                         ▼
              Phase 4 (编辑器工具)
                         │
            ┌────────────┼────────────┐
            ▼            ▼            ▼
        调试窗口      验证工具      监控器
            │            │            │
            └────────────┼────────────┘
                         ▼
              Phase 5 (测试体系)
                         │
            ┌────────────┴────────────┐
            ▼                         ▼
        单元测试                   集成测试
            │                         │
            └────────────┬────────────┘
                         ▼
              Phase 6 (文档示例)
                         │
            ┌────────────┼────────────┐
            ▼            ▼            ▼
        API文档      Basic示例    RPG/MOBA示例
            │            │            │
            └────────────┴────────────┘
                         │
                         ▼
                   v8.0.0 发布
```

---

## 附录：v8.0+ 未来扩展规划

### 短期扩展（v8.1-v8.3）

| 版本 | 功能 | 说明 |
|------|------|------|
| v8.1 | Roguelike示例 | 随机Buff、永久Buff展示 |
| v8.2 | Networking示例 | 网络同步完整示例 |
| v8.3 | ECS架构支持 | 大规模Buff场景优化 |

### 长期规划（v9.0+）

| 方向 | 功能 | 目标 |
|------|------|------|
| AI集成 | Buff决策系统 | NPC自动Buff策略 |
| 可视化 | 节点编辑器 | 可视化Buff逻辑编辑 |
| 云同步 | 云端存档 | 跨设备Buff同步 |

---

## 总结

v8.0优化流程采用**新优先级策略**：

1. **先做独立功能** - 快速见效，可并行开发
2. **再做功能升级** - 在稳定基础上增强
3. **然后性能优化** - 基于测试数据针对性优化
4. **接着编辑器工具** - 功能稳定后开发工具
5. **最后测试文档** - 完善质量保证和交付

这种策略的优势：
- ✅ 快速看到成果，保持开发动力
- ✅ 降低风险，避免前期过度设计
- ✅ 数据驱动，性能优化有依据
- ✅ 工具后置，避免反复修改

**预计总工时：8周（2个月）**
**目标：达到100%生产就绪度，成为行业标杆级Buff系统**